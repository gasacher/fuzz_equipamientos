export function DemoFormBanner() {
  return (
    <p className="rounded-lg border border-[#e50914]/40 bg-[#1a0a0a] px-4 py-3 text-sm text-[#f2f2f2]">
      Vista de presentación: el formulario es el mismo que en el sistema real. Al guardar volvés al
      listado; en esta URL los datos no se persisten.
    </p>
  );
}
